import 'package:flutter/material.dart';

class HealthList extends StatelessWidget {
  const HealthList({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Health List - Coming Soon'),
    );
  }
}